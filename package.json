{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Expense Tracker - Full Stack Project",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "start": "cd server && npm start"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
