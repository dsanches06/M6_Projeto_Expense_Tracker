{
  "buildCommand": "npm install && cd server && npm install && cd ../client && npm run build",
  "installCommand": "npm install && cd server && npm install && cd ../client && npm install",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/client/api/index.js"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "VITE_API_URL": "/api"
  }
}
